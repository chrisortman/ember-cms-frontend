
import { indexToPosition } from 'cms/helpers/index-to-position';
import { module, test } from 'qunit';

module('Unit | Helper | index to position');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = indexToPosition([42]);
  assert.ok(result);
});

